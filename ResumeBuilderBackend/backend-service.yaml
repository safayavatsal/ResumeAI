apiVersion: v1
kind: Service
metadata:
  name: backend
  labels:
    app: backend
    project: resumeai
spec:
  type: LoadBalancer
  selector:
    app: backend
  ports:
  - port: 4292
    targetPort: 4292
